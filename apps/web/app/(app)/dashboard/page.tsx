import { Placeholder } from "@/components/shell/page-head";

export default function DashboardPage() {
  return (
    <Placeholder icon="◫" title="Dashboard">
      Curated by attention — the morning digest, vigilance items and tiles that
      learn what you check. Arrives once the surfaces it watches exist.
    </Placeholder>
  );
}
