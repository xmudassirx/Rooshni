import { Placeholder } from "@/components/shell/page-head";

export default function Page() {
  return (
    <Placeholder icon="◈" title="Website · Settings" chip="Later in Session 8">
      The eight settings tabs — being built later in this session.
    </Placeholder>
  );
}
