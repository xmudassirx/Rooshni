import { Placeholder } from "@/components/shell/page-head";

export default function Page() {
  return (
    <Placeholder icon="◔" title="Feedback" chip="Later in Session 8">
      The grant-gated feedback surface — being built later in this session.
    </Placeholder>
  );
}
