import { Placeholder } from "@/components/shell/page-head";

export default function AutomationPage() {
  return (
    <Placeholder icon="⟳" title="Automation">
      Workflows are data — readable, toggleable, and only ever changed through
      your stamp. Arrives with the workflow engine.
    </Placeholder>
  );
}
