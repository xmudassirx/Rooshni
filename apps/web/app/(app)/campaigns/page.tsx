import { Placeholder } from "@/components/shell/page-head";

export default function Page() {
  return (
    <Placeholder icon="⬆" title="Campaigns" chip="Later in Session 8">
      Segments as saved views and the saved blocks library — being built later in this session.
    </Placeholder>
  );
}
