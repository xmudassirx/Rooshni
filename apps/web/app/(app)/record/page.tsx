import { Placeholder } from "@/components/shell/page-head";

export default function RecordPage() {
  return (
    <Placeholder icon="≡" title="The Record">
      Append-only, forever — every actor writes here, no one edits, not even
      you. The screen over the events ledger arrives in a later session.
    </Placeholder>
  );
}
