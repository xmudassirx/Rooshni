import { Placeholder } from "@/components/shell/page-head";

export default function Page() {
  return (
    <Placeholder icon="▣" title="Billing &amp; usage" chip="Later in Session 8">
      Plans, meters and caps — being built later in this session.
    </Placeholder>
  );
}
