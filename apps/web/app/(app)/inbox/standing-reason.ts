/**
 * The standing shadow-mode rejection reason (Session 12). Shadow mode means
 * the existing pipeline already handled the client, so Light's draft is
 * refused — this exact wording lands on the row and The Record.
 */
export const STANDING_REASON = "Shadow mode — handled by existing pipeline";
