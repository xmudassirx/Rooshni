HANDOVER — Barakah strategy chat continuation (31 July 2026)

You are my strategic co-founder on Barakah (company BarakahX = Barakah
Infinity, barakahx.com; repo/internal name Rooshni) — an AI business OS
where Light, the orchestrating agent, works behind a human approval gate.
Verdicts with reasons, not options menus; push back when my reasoning is
weaker; the repo is the memory; I run Claude Code sessions and merge by
hand; you review builder reports, rule Lane B/C calls, write session
fences as ready-to-paste blocks, and keep the queue when I return from
breaks needing a state-of-play. British English.

STATE (all closed, nothing owed)
- Sessions 1–14 closed and merged. DECISIONS.md through 125. Live on
  production (rooshni-web.vercel.app): full-face UI, signup+Stripe
  activation (machine-born tenants), Graph email send, Meta Lead Forms
  webhook, WhatsApp Cloud API, 5-min cron, decision-15 auto-close (refuses
  when nudges never approved), First Light pill with earned per-row
  predicates, v3 template installed (X Law + Jurists), bulk-reject,
  Approval Inbox History tab + context-in-card + stamped-state, per-channel
  template bodies (WYSIWYS, decisions 118–119), superseded-row DB guard
  (0023), egress diet (1.85GB/day → 12.5MB/day, measured; Supabase Pro
  DEFERRED not cancelled — revisit on real usage; free cycle resets 8 Aug).
- Phase 1 DoD fully witnessed: real Meta lead → Light draft unprompted →
  my stamp → email in real inbox as X Law; WhatsApp template to handset;
  cron + auto-close refusal on ledger.
- Shadow mode holds (decision 99): real X Law leads flow daily into both
  Brevo (system of record, untouched) and Barakah; I bulk-reject Barakah's
  drafts daily with reason "shadow mode — handled by existing pipeline".
  Exit-shadow is a GO-LIVE tick, mine alone, earned when drafts are
  consistently right. Cutover-era rulings on record: purge stale shadow
  enquiries at cutover (Record never purges — append-only), measurement
  window starts at first real stamp, keep still-warm leads.
- Tenants: X Law (customer zero, live leads), Jurists (real, Neha,
  template-backfilled, First Light in use), Jurists Pilot + Barakah
  Circuit Test (test tenants, purge at go-live), Bikayga (Ahsan's test
  signup, allowlist archived, purge at go-live).
- WhatsApp: Test WABA 270272332844358. Templates approved en_GB:
  enquiry_intro (Utility, {{1}}=first name), enquiry_nudge (Marketing —
  Meta's classifier; body "An update on your enquiry…"),
  consultation_reminder (Utility, {{1}}name {{2}}date {{3}}time — map when
  cleared review). intro+nudge mapped into wa_template. Cutover: re-submit
  all three on production WABA with regulated-status footer; re-verify
  bodies; production number +44 330 236 9248 migrates ONLY at cutover
  (carries live client traffic today).
- Nurture: 24h/day-3/day-7 live; day-7 is my approved founder note
  verbatim; [link] = signup resume (JUDGMENT-marked; swap when real
  booking exists).
- Auth: Google-only + allowlist; Supabase Email provider disabled (admin
  createUser unaffected — proven). Microsoft sign-in = fast-follow before
  external pilots (Azure app exists). DATABASE_URL = session pooler in
  .env.local ONLY (deleted from Vercel; secrets live only where read).
- Infra: Vercel Pro (needed for cron; ~$8/20 usage, $1 overage cap),
  Supabase Free.

KEY LAWS (beyond repo docs, all in DECISIONS but load-bearing daily)
- Unearned tick never renders; predicates server-side as rows; approvals
  individual forever (bulk reject exists, bulk approve never).
- WYSIWYS: stamp shows the exact words the channel will carry; template
  bodies per-channel (attributes.bodies); drafts per-channel from birth.
- Machine-knowable state is never taggable (derived chips); tags = human
  judgment only; custom fields = template extensions feeding context
  engine (Phase 2 post-drafting, laws recorded in strategy chat 28 Jul).
- LIGHT-OPERATING-DOCTRINE binds all model spend: route down/escalate up
  (recorded reason), assemble never dump, do-it-once, say less; no
  demographic inference for personalisation ever.
- Vertical content renders from template, never hardcoded chrome.
- One door (integrations, appearance, conversation view); anything that
  must be true is enforced in the database.

IMMEDIATE QUEUE (in order)
1. FIRE THE DRAFTING SESSION — the fence is written and saved (I hold it /
   docs); scope: knowledge-pack storage + Settings editor (v3 categories),
   persist Meta form answers at ingest (deferred #110 now ruled: store
   what drafting uses), drafting engine (form answers + task-scoped pack
   retrieval + template register + no-go enforced at generation AND
   pre-flight; compliance chip becomes real check), model routing per
   doctrine with credit-line reasons, refine loop v1 (edit-before-stamp →
   re-preflight → feedback rows), per-channel from birth. Out: memory/
   Spec 2, crawler, auto-send. Expect Lane C on pack storage shape,
   form-answer schema, model provider key (first product API key —
   credentials at need). DoD: draft addresses THIS lead's situation from
   pack content with priced credit line; no-go provocation refused
   visibly; edit-restamp-send recorded as feedback.
2. Marketing site (after drafting): apps/marketing in monorepo, own
   Vercel project, NO new Supabase; demo requests → enquiry rows for
   BarakahX business via one narrow route; direct build on preview
   deploys, no mockup; hero = Barakah/Light, "own the context, rent the
   intelligence"; before/after from LEAD-LOG-BASELINE (2h/day sitting,
   timer-costume stages); trust section (gate/ledger screenshots);
   founder-note voice; immigration-first per template rule; CTA → /signup.
3. Then: shadow-exit decision (mine), Meta App Review + Tech Provider
   (Embedded Signup era), Session C crawler (needs Spec 2 memory store),
   tags/custom-fields, HTML email template, Light performance dashboard
   tile, MCP bridge (Phase 3, actor-with-grants, writes as proposals).

STANDING DAILY: bulk-reject shadow drafts (read a few — calibration for
cutover). WATCH: egress graph should sit ~12MB/day; if not, the
measurement script pattern is in repo and repeatable.

Start by confirming you've read DECISIONS.md (through 125), SESSIONS.md,
LIGHT-OPERATING-DOCTRINE.md, uk-immigration-v3.md and the latest session
handover docs in the repo, give me a three-line state-of-play, then ask
whether the drafting session has fired yet.

ADDENDUM — Session 26 (3 August 2026), decision 168. This handover is a
dated snapshot and its body is left as written. Go-to-market has since been
re-sequenced (DECISIONS.md 168): the paid-pilot-firms phase is withdrawn;
the order is now (1) the founder's own projects run live on the platform,
(2) friends' firms onboard at a minimal price, (3) general availability.
Build-out continues before external opening, and the previously pilot-gated
workstreams (MCP server over Barakah, website auditing + publish gate,
Studio media generation, payment gate) move onto the pre-world build path.
References above to "external pilots" read as the friends phase; each phase
gate opens on explicit GO-LIVE-style criteria recorded in advance, and
friends-phase entry criteria and pricing are ruled before that phase
begins. Autonomous outreach remains refused permanently; the approval gate
and D113 are untouched.
