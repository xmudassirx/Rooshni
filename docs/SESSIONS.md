# Session ledger

One line per session: **number · date · one-sentence scope · landing SHA(s) · close status.**
Appending the session's own line is part of every close (PLAYBOOK §3.2 step 8, §8; session-close
skill item 7). Landing SHAs name the session's content commits, oldest…newest; the close commit
carrying a session's own ledger line is named in its close report.

Entries marked **®** are reconstructed backfill, written at Session 7 and verified against git
history and `docs/DECISIONS.md` only — dates are commit dates, sign-off dates per DECISIONS
headers where they differ. Commit `c077223` (Phase 0 specs, master context, mockup) predates
Session 1 — a founder commit, no session. The two method sessions of 9 July 2026 predate the
numbering of method work and carry no number.

<!-- JUDGMENT: ledger format fills beyond the ruled columns — the ® reconstruction marker,
     unnumbered rows for the two pre-ledger method sessions, and first…last SHA spans — are
     Lane B; the ruling named the columns only. -->
<!-- JUDGMENT: this session takes number 7, read from the backfill — sessions 1–6 verified
     numbered in DECISIONS.md, the method sessions verified unnumbered; the prompt left N
     symbolic by design. -->

| # | Date | Scope | Landing SHA(s) | Close |
|---|------|-------|----------------|-------|
| 1 | 8 Jul 2026 | Spec 1 schema — monorepo scaffold, six primitives, RLS + structural gates, seed, live apply | `abeabe9`…`48a3155` | closed; signed (DECISIONS 1–5) ® |
| 2 | 8 Jul 2026 | Spec 3 permissions engine — levels as data, tool registry, grants, gated stage moves | `b080b89`…`11c18ef` | closed; signed (DECISIONS 6–15) ® |
| 3 | 8 Jul 2026 | The Approval Inbox — view over pending states, readiness pre-flight, closed approve/reject pipeline | `3d1fcb3`…`bba87e6` | closed; signed (DECISIONS 16–22) ® |
| 4 | 8–9 Jul 2026 | First UI slice — Tailwind/shadcn foundation, app shell, Enquiries pipeline, Approval Inbox UI | `5dc857b`…`436833b` | closed; signed (DECISIONS 23, since retired) ® |
| 5 | 9 Jul 2026 | Authentication — allowlist door, Google sign-in, middleware gate, production sign-out/OAuth fixes | `544d826`…`734c272` | closed; signed (DECISIONS 24–28) ® |
| — | 9 Jul 2026 | Method: playbook v1.0 install + verification close-out (incident 1) | `99bd2f2`…`35a24b0` | closed; signed (DECISIONS 29–31) ® |
| 6 | 9 Jul 2026 (signed 12 Jul) | Spec 4 workflow engine — definition door, run state machine, MVP workflow as data, tick entry points | `99d9a74`…`77e69ba`; near-miss log `5ee4178` | closed; signed (DECISIONS 37–56) ® |
| — | 9 Jul 2026 | Method: skill hardening, incident-2 tightening — ran in its own granted worktree | `7bbe683`…`8b03faa` | closed; signed (DECISIONS 32–36) ® |
| 7 | 12 Jul 2026 | Method: finding-sweep — pathway X-ray fixes (CLAUDE.md, PLAYBOOK), this ledger created, `--allow-dirty` close gate | `4025795`…`44049f3` | closed; approved in-prompt (DECISION 57) |
| — | 15 Jul 2026 | Design Pass 3 — master mockup v2 (33 views), closed 2026-07-15; authority file `docs/design/master-mockup-v2.html`; amendments in `docs/design/AMENDMENTS-PASS3.md`; recorded by docs session D1 on branch `docs/pass3` | `be7f78a`…`3b073a8` | closed; signed in-prompt (DECISIONS 58–63) |
| 8 | 16 Jul 2026 | Full-face UI — design pass 3 tokens (Frost default, accents, prism Light), 24 remaining Phase 1/2 views over live rows or honest empty states, plus the founder-ordered fix round (ask modal, Appearance/General tabs, one-door Aa amendment, green-as-chrome sweep), on branch `ui/s8-faces` (unmerged, awaiting click-review) | `264814a`…`5d7fcb7` | open — awaiting founder click-review; rulings signed (DECISIONS 64–75) |
| 9 | 17 Jul 2026 | Onboarding foundations — 0020 (signup holding fields, platform-scope events, stripe_events, first_light_predicates, activation/deletion doors), Barakah-branded signup pair outside the shell, fail-closed idempotent Stripe webhook, pre-active lifecycle on the existing cron (Resend platform mail); merged, 0020 applied live, test-mode wiring + full DoD circuit run on production, founder-review quick fixes | `fa8dc4c`…`2aa381f` | closed; DoD ① witnessed by founder, ② ③ accepted per circuit report; rulings signed (DECISIONS 79–89) |
| 10 | 17–20 Jul 2026 | Connect to the world — 0021 send door (mark sent/failed service-only, WhatsApp session-window pre-flight, meta_webhook_events), real Graph + WhatsApp dispatch with quiet hours replacing the Session 6 STUB, Meta Lead Forms webhook (handshake, signature-before-parse, leadgen idempotency, semantic-stage ingest + Conversations thread + workflow trigger), Vercel cron on the tick, decision-15 auto-close refusal; merged, 0021 applied live, full wiring (page binding + page-level subscription via API, Mail.Send least-privilege adapter, client-facing subject law, neutral greeting v2), 16 real leads flowed in shadow mode | branch `cbfc6ee`…`b79bf0f`; main `91c4983`…`4458509` | closed; DoD ① ② ③ founder-witnessed 20 Jul (email + WhatsApp arrivals, real-lead ingest, cron-fired timer + auto-close refusal); rulings signed (DECISIONS 90–99); shadow mode holds (GO-LIVE: exit tick) |
| 11 | 24 Jul 2026 | First Light — 0022 template store + v3 install at activation (founder-ruled contacted rename, businesses.template_id real), the Contacted transition trigger on first dispatched outbound, seed reconcile of the X Law install (in place, evented, retired stages archived), First Light pill/panel with server-earned ticks + basics-confirm modal, true empty states, nurture days 3/7 + unsubscribe, Approval Inbox trio (context-in-card, History, transient stamped state); 0022 + seed applied live | `10ea03a`…`dce06f1` on `ui/session-11-first-light` | closed; merged to main at `dee46cb` (founder); rulings signed (DECISIONS 100–112); at close, founder-ordered template backfill run + evented on the Session-9-activated Pilot Test tenant (v3 install, workflow actor + grant, basics copy) |
| 12 | 30 Jul 2026 | Bulk rejection for the Approval Inbox — selection mode on stamps owed (checkboxes + select-all-visible, communications only), one shared reason looped through the single rejection pipeline so every refusal is its own evented entry on The Record, standing "shadow mode" quick-reason chip in both reject dialogs; bulk approval constitutionally absent (decision 113, §7 individual-stamp entry) | `ca9fcf9` on `ui/session-12-bulk-reject` | closed; merged to main by founder (fast-forward — main at `5676756`); rulings signed (DECISIONS 113–116) |
| 13 | 30 Jul 2026 | Fix round — basics-confirm per-row law (decision 84): shared required-set resolver (canonical six when no template install), fail-closed evaluator, explicit not-applicable act per valueless row (evented), confirm-all-remaining over visible values only, unearn drive mode; Jurists' unearned tick struck live (19:02) | `5d2ed31`…`6790a41` on `ui/session-13-basics-per-row` | closed; merged to main at `9bec6e2` (founder; the merge landed with an unresolved conflict marker in this ledger — repaired at the modal chore, 30 Jul); rulings signed (DECISION 117); DoD witnessed 20:49 — six rows individually addressed, the flip payload names all six, and earlier single confirms did not flip (the run rode the still-deploying pre-merge code, made correct by the backfilled template; the fixed evaluator ships from `9bec6e2`); at close, founder-ordered backfill run + evented on the REAL Jurists tenant, Neha's signup (v3 install `019fb4ab-5e28`, template_id set, workflow actor + enquiries grant, basics copy aligned, second strike + task reopened) |

| 14 | 31 Jul 2026 | Egress diet + auth forensics — one tick measured live at 1.29MB/412 requests (trigger scan re-reading full Meta payloads, ~100 shadow-blocked runs dragging full bundles, stub-era bodies re-read every minute), dieted to 43.5KB/9 requests (column selects, batched stamped-drafts check, ids-before-bodies dispatch sweep); cron to */5 (DECISION 121, founder-ruled in-prompt), deployed READY on Vercel; projection 12.5MB/day vs 160MB/day budget — free tier suffices; bikaygapl@gmail.com traced to Ahsan Raja's activated test-mode signup (Bikayga tenant, admin-API-created auth user, never signed in — allowlist row + active account EXIST, cleanup recommended not executed); Email-provider disable verified harmless to activation (admin createUser 200, public /signup 400 email_provider_disabled) | `66206a7`, `ba7a35f` | closed; cadence ruling pre-approved in-prompt (DECISION 121); forensics read-only + one synthetic auth user created and deleted for the provider-disable proof; at close, Lane B 1–3 approved and Bikayga ruled (DECISIONS 122–125) — allowlist row archived live via the new `allowlist:archive` evented chore (event `019fb7d1-f883`), tenant + auth user to the go-live purge |
| 15 | 31 Jul 2026 | Query-aware drafting — 0024 drafting declarations (knowledge category + route vocab, engagement form_answers; activate_signup re-issued), 0025 draft_feedback (append-only refine signal), 0026 compliance pre-flight (compliance_required stamped at birth, server-only recorded checks, fail-closed gate per ruling C-2); the drafting engine (channel first per 119, WhatsApp template verbatim per 118, email composed against form answers + task-scoped published pack + no-go register in-prompt; Haiku-class floor / Sonnet-class escalation with recorded reason in one router module; credit line on the row and priced on The Record — events.cost's first producer; transient/permanent provider-failure split, never a stub fallback); Meta ingest persists form_answers, evented backfill re-fetched 103/104 shadow-era leads by stored leadgen id (1 visible Graph failure, zero payload-column reads); Settings → Knowledge tab (the pack's one door, vocab rendered from the installed declaration), inbox credit line + real COMPLIANCE chip + edit-before-stamp (re-preflight on the exact edited words, feedback captured on edits and rejections); circuit-proven live: one floor-model generation, clean recorded check, draft pending in the inbox; 195 smokes green | `85d8df6`…`5160d92` + close commits on `s15-drafting` | closed pending merge — DECISIONS 126–131 on in-prompt/quoted authority, Lane B 1–11 approved at the close review with riders (DECISION 132, incl. edited-body attestation semantics), the inbound-supersede engine recorded as the deferred next-session ruling (DECISION 133), firm-sign-off register fix landed with its refusal smoke; click-review fix round: 0027 submitted_at (the waiting clock is the client's, immutable across edits — DECISION 134) + the neutral edited-by chip; awaiting founder click-review + merge, then the C-3 second backfill run |
| 16 | 1 Aug 2026 | Inbound supersede engine + live inbox — 0028 inbound capture (WhatsApp Cloud webhook + Graph 5-min-cron poll → received communications rows, claim-table idempotency, service-window state + consent refresh on the thread; Mail.Read consent = the anticipated Lane C, console steps on GO-LIVE), 0029/0030 supersede engine (one-pending-per-engagement-per-channel guard indexes, superseded terminal/frozen/never-deleted, service-only supersede pipeline inheriting the client's clock through the 0017 door, same-transaction human-reply auto-supersede), settle windows as durable thread-row timers (instant/1/3/5 default 3, per-conversation override, restart per inbound, timeScale-proven), proactive Conversations drafting (reply register against the full conversation + form answers + task-scoped pack; Ask Light bypass; pause-only toggle; direct send = decision-21 insert-at-approved wired), Anthropic prompt caching on reply calls with cache figures on the credit line, approver sign-off resolved WYSIWYS at render+stamp via one deterministic resolver (Settings → General gains the drafting-policy trio), 0031 live inbox (one Realtime subscription, no polling, toggleable arrival tone); DECISIONS 135–141 on in-prompt authority | `3657c49`…`2295bfd` on `s16-supersede` | closed pending merge — 215 smokes green; awaiting founder merge + the deferred witnessed circuit (`npm run circuit:supersede` stages the pre-check); Lane B calls awaiting sign-off in the close report |

| 17 | 1 Aug 2026 | Marketing site — apps/marketing in the monorepo (own Vercel project, staged as GO-LIVE console steps): seven surfaces in the founder-note register (home with LEAD-LOG-BASELINE before/after + The Record fragment, product gate/ledger/drafting story, pilot pricing on request, founder note, pilot-era privacy + terms, /demo), ONE narrow write path (/api/demo → contact + enquiry at New for the demo business, workflow-actor attributed, evented, honeypot + rate limit, fails closed on missing config — BarakahX tenant verified absent on live and staged, never invented); no analytics, no cookies, rendered copy dash-free; mid-session Lane C stop on a folder collision with session 18 (founder-ruled resumed; parallel checkouts now law, DECISION 143), s18's canonical-URL seam honoured by value (NEXT_PUBLIC_APP_URL, no hardcoded hosts) | `9c9658a`…`6103cb2` on `s17-marketing` | closed pending merge — awaiting founder click-review of the preview once the Vercel project exists, the BarakahX tenant ruling, and the Lane B sign-offs in the close report |

| 18 | 1 Aug 2026 | Canonical URL + register chores — NEXT_PUBLIC_APP_URL seam through one helper (canonicalOrigin: env var when set, request-derived otherwise; nurture/resume links, [link], unsubscribe, Stripe success/cancel, auth redirects rerouted; zero VERCEL_URL/vercel.app production hits existed) + the register rule (no em/en dashes in generated client-facing bodies — prompt lines, composition screen, 4 refusal smokes; DECISIONS candidates 142–143); mid-session Lane C: Session 17's parallel work appeared in this folder — founder ruled the parallel-checkout law, stopped 17, declared the foreign paths expected-dirty, granted hunk-scoped staging | `32c6734`…`8cfdacf` on `s18-chores` | closed pending merge — 219 smokes green; Lane B awaiting sign-off in the close report |

| 19 | 1 Aug 2026 | Multi-touch workflow + HTML email — 0032 (route_guide vocabulary + declared-ATTACHMENTS pre-flight: existence, linkage, 8MB ceiling, fail closed), booking_url in Settings → General with [link] substitution at composition (WYSIWYS, fail-fast on an unconfigured token), the HTML email dress (one deterministic renderer for stamp preview + dispatch + parity smoke; sent rows store the exact dispatched document with attributes.plain_body preserving the approved words), route-guide documents in Settings → Knowledge (PDF ≤8MB, private Supabase Storage bucket, publish-gated; the drafter attaches the route-matched PUBLISHED guide; Graph carries ≤3MB inline, 3–8MB behind the Mail.ReadWrite GO-LIVE consent), the multi-touch intro as step-config companion_channels (WhatsApp template companion, two individual stamps, run blocks on the email stamp alone; new definition version via chore:install-multitouch-intro, decision 40); mid-session founder ruling folded in: inbound WhatsApp = transactional consent (ingest create-or-refresh scoped to the matched contact + evented backfill:wa-consent chore); DECISIONS candidates 145–149 on in-prompt/quoted authority | `5f17ff0`…`3504596` on `s19-touch` | closed pending merge — 234 smokes green incl. the four ordered coverage items; DoD deferred to the founder's testing window; Lane B awaiting sign-off in the close report; live untouched (0032 apply + chores are founder steps on GO-LIVE) |
| 20 | 1 Aug 2026 | The doors — Microsoft sign-in beside Google (equal-weight two-provider screen, provider recorded honestly on auth.signed_in, the identity-linking fork failing closed to the holding page; allowlist + RLS provider-blind, parity smoked; Supabase Azure enable + Azure console steps staged on GO-LIVE) and Google Workspace as the second tenant-mail pipe (provider-neutral mailbox boundary, raw-fetch Gmail adapter with self-minted Message-ID rfc822 carriage, 0033 gmail_mail_events claims, one shared poll engine on the tick, per-business absolute carriage selection with the owner-gated choice in Settings → Integrations, wire-inbound --gmail + gmail:authorize; staged DoD — fail-closed build, harness-proven, morning console steps on GO-LIVE); mid-session founder hotfix folded in as standalone `ec1d8e9` (serverActions 10mb over the 8MB law + guide documents on any route entry, DECISIONS 150–151 on quoted/delegated authority) | `7a63ded`…`a5d97e9` on `s20-doors` | closed pending merge — 244 smokes green incl. the three ordered items; WS1 DoD awaits the Supabase Azure tick, WS2 DoD staged for the founder's morning; Lane B awaiting sign-off in the close report |
| 21 | 1 Aug 2026 | Small chores sweep — the stuck-definition escape hatch (0034: owner-only withdraw of a pending_approval workflow definition; terminal/frozen/never-deletable, reason on the row, `workflow.definition_withdrawn` on The Record, the single Withdraw control on the inbox card + the History withdrawn arm; the live v2 withdrawal left to the founder as the DoD witness), chore 2 confirmed already captured (DECISION 151 records client-direct signed-URL upload as the future shape — annotation added, no new line), chore 3 sweep (zero TODOs in the repo; one stale s15 deferral clause in workflow.ts closed against decision 140; the two 0019 comment-only lint artefacts reflowed with check_migration green; the three unsigned s19/s20 "awaiting sign-off" marks listed and untouched) | `421b03e`…`e2bed1d` on `s21-sweep` | closed pending merge — 261 smokes green incl. the three ordered items; DoD deferred to the founder's witnessed withdrawal after the 0034 apply + deploy (GO-LIVE); Lane B awaiting sign-off in the close report |
| 22 | 1 Aug 2026 | Phase 2 completion — the Meta Conversions loop (founder-ruled mapping Schedule/Purchase server-side from stage_history, hashed em/ph only with the payload-as-sent on The Record, retryable visible failures, fire_conversion STUB retired, spend_records' first producer fail-closed on ads_read, the one-door Conversions row default OFF), Billing & usage real (priced cost blocks at list rates + recorded fx from both generation producers, metered-cost page by day/action, ai_budget soft/hard caps owner-set — hard cap refuses generation visibly in the s15 lane, sends continue, gate untouched), the Light performance tile (existing truth, pure computation fixture-proven), the mobile sweep (thumb targets, one-handed stamp row, sign-in verified at 375px; The Record declared desktop-only), and the mid-session Workstream 5 read-layer diet (5a inbox pagination oldest-wait-first + full-set server-side bulk reject, 5d contacts/pipeline/history windows, 5e counts-law + bound-pinning tripwire; 5b/5c honestly not built); diagnostic: the ~116-draft burst confirmed from the ledger as v3 replaying the historical trigger backlog (per-definition-id consumption, no activation cutoff) — activation semantics flagged Lane C | `ffd8f37`…`e436aa0` | closed pending merge — 281 smokes green; DoD staged for the founder's testing window; Lane B awaiting sign-off in the close report; live untouched (no migration this session) |
| 23 | 2 Aug 2026 | UX integrity + mobile + polish — WS1 approval-gate information integrity (1a THEY SAID: the reply draft's inbox card renders every inbound since the last dispatched outbound above the answer; 1b the inline stamp in Conversations invoking the SAME server acts, WYSIWYS-resolved bubble, withheld-when-blocked; 1c 0035 thread unread as a stored generated column + accent sidebar badge from a COUNT aggregate + the s16 tone on inbound under the same toggle; 1d cap semantics VERIFIED raw — the founder's failed DoD was display rounding, fixed with formatMeteredGbp 3dp-under-1p + smokes), WS2 Conversations rebuilt to the Messenger shape with the s22 5c deferral landing (0036 trigger-maintained last_activity_at, paged thread list, bounded tail + older-on-upward-scroll, Realtime appends without refetch, phone full-screen list/thread with back control, phone-frame view demoted to opt-in), WS3 The Record's 5b infinite scroll (cursor windows, day sections merging across edges, stacked mobile row), WS4 the ten-item defect sweep (opaque popovers/drawer/topbar, task title wrap, open-count header, 4d task cancellation — 0037 terminal state + the task_cancellation inbox arm + manager gate on owner/settings.team, contact card one-line, pipeline search server-side bounded, settings mobile stacking, auto-growing studio box, website pages POSITIVE whitelist with no public renderer confirmed, featured-image upload/library + soft publish warning), WS5 knowledge polish (attachment chips everywhere a file rides, the read-only entry view with version history + signed download, slug-prefixed object names), WS6 trigger consumption per workflow KEY (0038 claims table backfilled keep-earliest, claim in the run's transaction — replays structurally impossible, activation frontier stamped by the gated approve, both ordered smokes verbatim, chore:cancel-replay-runs staged for the founder); plus the founder's mid-session PRIORITY fix folded in as its own commit (the conversions stage lookup filtered stage_definitions by a business_id column that does not exist — resolved via the engagement's engagement_type_id in the sweep, fireEngagementConversions and circuit-conversion; v3-installer-shaped fixture smokes + a tripwire fencing the phantom column off) | `fc23222`…`2fe8bf9` on `s23-polish` | closed pending merge — 297 smokes green incl. the ordered WS6 pair and the 1d cap smokes; production build clean; DoD staged for the founder's witnessing window; Lane B awaiting sign-off in the close report; live untouched (0035–0038 apply + chore:cancel-replay-runs are founder steps on GO-LIVE) |
| 24 | 2 Aug 2026 | Defect-trio fix round (founder-ordered) — the silent live-inbox badge (the s16 LiveInbox Realtime lifecycle rewritten: setAuth before every join, subscribe status watched with capped-backoff rejoin, onAuthStateChange re-arming the socket, reconciling refresh on every (re)join and on tab return; tone/refresh decision extracted to the pure live-inbox-rules module, founder-ordered live-inbox smoke landed), quiet-hours legibility (post-stamp card + Conversations bubble read "Approved — held by quiet hours · sends [time]" in neutral chrome from the ROW's scheduled_for, never a recomputation) with SEND NOW for stamp-holders (0039 override_quiet_hours_hold — human + owner-or-approvals.comms execute, only an approved held row, TIMING only with the 0021 door untouched, who/when on the row, communication.quiet_hours_overridden on The Record, dispatcher never re-holds; four door smokes), and business hours real (Settings → General owner-gated editor of a simple daily send window writing settings.quiet_hours — ONE SOURCE, founder-ruled — plus the derived business_hours display string; "default — not yet set by you" until firm-set via isQuietHoursSet, proven) | `98a30d4` on `ui/defect-trio-quiet-hours`; merged to main at `df5ecc4` (founder) | closed; 308 smokes green, typecheck clean; 0039 apply staged on GO-LIVE; the quiet-hours template-default Lane B question carried open from the close — ruled at Session 26 ® |
| — | 2–3 Aug 2026 | Defect-pair fix round (founder-ordered) — fail-loud reaches every surface (a communication at status failed wears RED wherever the message appears, with the recorded attributes.send_failure reason inline — the Conversations bubble with author-side alignment kept per decision 78, the enquiry timeline card, and the Approval Inbox History arm where communication.send_failed joins HISTORY_ACTIONS as a red decided fact) + RETRY for stamped-but-failed (0040 retry_failed_communication — human stamp-holder door, only a FAILED row, failed→approved with the ORIGINAL stamp asserted and preserved and the body untouched — WYSIWYS proven — the 0008/0017/0026 triggers re-firing inside the transition; attributes.send_failure remains beside the new send_retry who/when; communication.send_retried on The Record; inline dispatch re-attempts; one shared RetrySendControl serves all three faces, rendered only for stamp-holders on still-failed rows; five smokes) | `61e3d76`…`91f7ecc` on `ui/defect-pair-failed-sends`; merged to main at `af169d3` (founder) | closed; 313 smokes green, typecheck + production build clean; 0040 apply staged on GO-LIVE; rulings signed (DECISIONS 162–163) ® |
| 25 | 3 Aug 2026 | Generation-failure visibility + register retry-once — light.draft_generation_failed refusals (register, compliance, provider, cap) render fail-loud in the defect-pair grammar: the Conversations thread interleaves the thread's refusals into the flow (red card, RECORDED reason verbatim, the standing refusal carrying the live "Ask Light to draft again" door), the enquiry timeline resolves refusals through the engagement's own threads and runs (red pin + reason; comm_thread refusals link across to the conversation, workflow_run refusals carry no dead control per decision 116), The Record describes both kinds; register retry-once (B1 follow-up): the Session 18 screen throws RegisterBreachError and both production compose sites route through the shared composeWithRegisterRetry — exactly one retry with the violation fed back into the regeneration prompt, light.draft_register_retried evented BEFORE the second attempt, both attempts metered on the credit line, a second breach standing visible in the existing draftGenerationFailed lane, never a loop, never a lease retry | `bd9cd96`…`5dc3e7f` on `ui/session-25-generation-failure-visibility` | closed pending merge — 317 smokes green incl. the four ordered items; typecheck + production build clean; no migration, live untouched; Lane B awaiting sign-off in the close report; NOTE for founder bookkeeping: DECISIONS.md ends at 161 (the prompt's 162/163 defect-pair entries are not in the file) and SESSIONS.md carries no line for session 24 or the defect sessions *(note resolved at Session 26 ®: DECISIONS 162–167 recorded by the founder at `47c41fa`; the session 24 and defect-pair rows backfilled above)* |
| 26 | 3 Aug 2026 | Docs true-up + small-fixes sweep — the SESSIONS.md backfill (s24 + defect-pair rows ® marked; s25's line already present, skipped), DECISION 168 recorded verbatim on in-prompt authority (go-to-market re-sequenced: founder's own projects live → friends' firms at minimal price → general availability; the paid-pilot-firms phase withdrawn; pilot-gated workstreams onto the pre-world build path) with the plan-language sweep (GO-LIVE, master context §4, STRATEGY-HANDOVER + ONBOARDING-HANDOVER dated addenda, v3 doc; laws and rulings 1–167 untouched), Record rows expanding in place with Open enquiry as a button INSIDE the expansion (pure record-row module, component smoke + disclosure tripwire), the real Paperclip on attachment chips (⎘ gone repo-wide), the read-only /files surface (Think section, 5e-windowed, cross-tenant RLS smoke both directions), the founder-ruled nudge ladder (nudge 1 T+1d WhatsApp, nudge 2 T+3d email, nudge 3 T+6d final, close ≈T+9d) as `chore:reissue-nudge-ladder` re-issuing v4 through the pipeline (D40/D102; the founder's GO-LIVE run with --approve-as-owner IS the stamp), and the quiet-hours default template-sourced (the installed declaration; the constant install-less only; dispatch hold + Settings render through one resolver) | `7694c2b`…`8351985` on `ui/session-26-docs-trueup-small-fixes` | closed pending merge — 323 smokes green incl. the three ordered items; typecheck + production build clean; no migration, live untouched; DECISIONS candidates for C4/C5/expanded-row drafted in the close report per §8 |
| 27 | 4 Aug 2026 | Returning leads + the three post-close rulings (D158-D161) - the frontier refined to the SUBMISSION (same leadgen id blocked twice over, untouched; a NEW leadgen id resolving to a known contact - email exact then phone, ambiguity resolves to no one - always processed), the neutral system marker into the contact's existing thread (direction internal, changed-fields diff highlighted, thread-to-top via 0036, unread via 0035, arrival tone via the live-inbox rules, excluded from the model transcript), the returning-context draft (open fork: the settle sweep treats a settled marker as a burst of its own; closed fork: the successor's enrolled run composes its intro with returning context on the repointed thread - no cold intro, no duplicate booklet by suppression, the WhatsApp companion standing down), enquiry linkage both forks (0041 predecessor_engagement_id, self-link refused, evented and rendered on both timelines; successors enrol on the ACTIVE definition), the attachment-honesty pre-flight naming the mismatch (0043 + the prompt belt), enquiry truth-timing (diagnosis from the live ledger: the classifier never wrote 019fc32a because it never existed - retrieval matched by text only; the page now says Classifying only while a read may still arrive, the timeline draft entries show attachment state), and route classification complete (0042 provenance door - human > form_answer > light > form_default enforced in the database, human final against machine writes; per-form defaults in Settings under the Meta row; Light classifying by riding the ONE drafting call's output, evented with its stated reason, gold chip; human reclassify dropdown from the declared vocabulary) | `3cb7bfa`...`ad7bee7` on `ui/session-27-returning-leads` | closed pending merge - 336 smokes green incl. the thirteen ordered items; typecheck + production build clean; 0041-0043 apply + the Spouse-form mapping are founder steps on GO-LIVE; Lane B awaiting sign-off in the close report |
| 28 | 5 Aug 2026 | Returning-lead channel refinement (D174, recorded verbatim on in-prompt authority) + whole-set contact search — the resolver keys on channel values only with the cross-channel conflict guard (email to one contact, phone to a different contact resolves to no one — fresh lead, deterministic, identities never merged) and the enrichment write (a matched contact presenting a new value on the other channel gains it as an additional contact_channels row, consent carried from the form, contact.channel_added evented with provenance, both s27 forks inheriting unchanged, idempotent by plan); the Contacts search queries the business's ENTIRE contact set server-side (display name + email/phone channel values, bounded legs per the WS4f precedent, debounced into the URL, results paginated with the query carried, live rows only, RLS proven against a second business without the business filter); the Conversations page-local search pattern reported as the ordered Lane C flag, not fixed | `bd3795a`…`0c459e8` on `ui/session-28-channels-and-search` | closed pending merge — 344 smokes green incl. the eight ordered items; typecheck clean, Vercel preview READY; no migration, live untouched; Lane B awaiting sign-off in the close report |
| 30 | 6 Aug 2026 | Honesty + controls sweep — D177 recorded verbatim (in-prompt authority) and implemented: Conversations whole-set server-side search (contact name + email/phone/whatsapp channel values, the s28 pattern, RLS proven both directions), the static gold pending-stamp dot beside the accent unread dot (177a, tier-1 no-motion), the rejected draft in stamp red with its recorded reason at thread bubble AND enquiry timeline (177b, the 0017 triple now selected by the thread window), the owner-only evented contact ARCHIVE with channel-cascade so resolution and the 0043 consent pre-flight exclude it while history stands (177c), disqualify cancels the live run through the 0019 door with mid-flight steps standing down (177d), the enquiry timeline flipped newest-first in the one pure buildTimeline module (177e), and the human stage control speaking the installed template's full vocabulary with the engine standing down when a human spoke last (177f); plus the queued fixes — WhatsApp settings card renders connected-via-environment truth (never an unearned negative), the reissue-nudge-ladder re-run STAMPS its own pending staging via the extracted chooseReissueAction (the D169 v4/v5 incident, harness-proven), and the skills' theme-count corrected to three/Frost-default per decision 62 | `ca7dabb`…`202c198` on `ui/session-30-honesty-controls` | closed pending merge — 356 smokes green incl. the twelve session-30 items; typecheck + production build clean; no migration, live untouched; DECISIONS 178 provisional (D176) — the founder's merge is the stamp |
| — | 6 Aug 2026 | Session 30 Workstream C micro-fix (founder-witnessed at click-review) — a successful contact archive now redirects SERVER-side to the Contacts book (the pure archivedContactRedirect target; the client push that raced into the archived page's honest 404 is gone) with the once-per-event "Archived · on The Record" confirmation in the shipped static toast pattern, self-clearing from the URL; smokes prove the redirect target, the static toast, and that the book offers no door to an archived contact | `c1a003d` on `ui/session-30c-archive-redirect` | closed pending merge — 357 smokes green; typecheck + production build clean; no migration, live untouched; no DECISIONS write (Lane B notes in the close report only) |
| 31 | 6 Aug 2026 | Drafting quality (D179, recorded verbatim on in-prompt authority as entry 179 — the prompt's 178 was occupied by s30's finalised provisional entry) — fees out of machine drafts (the absolute prohibition in BOTH generation prompts replacing the published-fee allowance, published_fees withdrawn from retrieval selection, the deterministic currency-amount screen refusing a generated body with the match NAMED in the 0043 grammar and joining the register retry-once lane, every live template body swept clean over REST including the two Meta-registered WhatsApp bodies — nothing to re-issue, nothing to flag — and the template sweep pinned in the harness); nudges compose as FOLLOW-UPS (the enquiry's genuinely sent outbound rides the nudge prompt as a dated per-channel summary, the follow-up register — never re-introduce, shorter than the intro, acknowledge prior contact only when something was truly sent); retrieval and attachments follow the RESOLVED route (resolveEngagementRoute completes the 0042 ladder plus Light's floor-tier pre-compose read BEFORE composition in both composers — evented engagement.route_read with priced spend, confident writes through the door with light provenance — retrieval and booklet selection keyed on the resolved route never raw text matches, unresolved route drafting route-neutral with no booklet; the D161b ride-along classification superseded and removed) | `3d635bf`…`d7879f2` on `session-31-drafting-quality` | closed pending merge — 364 smokes green incl. the six session-31 items; typecheck clean; no migration, live untouched (read-only REST sweep); Lane B awaiting sign-off in the close report |
| 32 | 6 Aug 2026 | Light's Memory (D181, recorded verbatim on in-prompt authority; D180 stamping s31's Lane B calls) — the memory_entries store with its law in the database (0044: append-only supersede chains where the only lawful update is the flip-and-chain-once transition, Light physically unable to author an instruction, the 800-token ceiling refusing with the count NAMED, one active fact per key); active instructions + facts riding BOTH compose paths with the riding entry ids on the credit line (the D179a fee line and D142 register line now SEEDED instructions, the deterministic screens staying code — softening the steering never the law, per the Q3 ruling); the ripple sweep on every fact edit (pure planner harness-proven: deterministic substitution corrections per in-platform carrier drafted BY LIGHT to the Approval Inbox at pending_approval behind the 0009 human-publish gate, template corrections applying as D102 re-issues and knowledge corrections as versioned edits at the stamp with a version-drift staleness guard, manual tasks naming the owed change per external surface, fail-loud review tasks when the old value is not found verbatim, website surfaces deferred visibly, ONE memory.fact_ripple_swept act); Memory the SINGLE HOME for sign-off/booking-link/opening-hours with Settings → General as faces writing through the memory door and settings only the transitional pre-seed fallback (Q1 option A); rejection reasons landing as observations with one-click evented human-only PROMOTE (the graduation supersede chain); the Memory surface rebuilt real (FACTS/BEHAVIOUR, edit/add/supersede with history rendered, token count beside the instructions, law-mirror notes, retired entries visible); the seed backfill as an evented TS door (seed:memory) staged on GO-LIVE | `aa02b0f`…`31de611` on `ui/session-32-lights-memory` | closed pending merge — 376 smokes green incl. the twelve session-32 items; typecheck + production build clean; 0044 apply (founder, pre-merge) + seed:memory run (founder go, post-merge) staged on GO-LIVE; DECISIONS 182 provisional (D176) — the founder's merge is the stamp |
| 33 | 7 Aug 2026 | Quiet hours: the choice at the stamp (D184, recorded verbatim on in-prompt authority) — the silent hold-until-window retired on the stamp surfaces in favour of the explicit choice: approving inside the destination's active quiet window now withholds the stamp server-side (before even the sign-off compliance write) and surfaces the ONE dialogue shared by the inbox card and the thread's inline approve (both render DecisionControls) naming the window with two acts — SEND NOW (stamp + the s24 0039 door + evented communication.quiet_hours_overridden, then carried) and APPROVE AND SCHEDULE (picker defaulting to the window's end, min now, any future time; the stamp lands with scheduled_for set plus the 0039-shaped marker, evented as the new communication.scheduled kind; the tick carries it at the chosen instant); the post-choice cards state the truth ('stamped · scheduled for 08:00' / 'stamped · sent now (quiet-hours override)'); Settings → General gains NO QUIET HOURS as a first-class owner choice (the D170 explicit-null path, evented, rendered honestly; a DISPATCH choice only per the founder's click-review amendment — the opening-hours fact stands, still sweeps GMB, and the row states both truths) beside the window config; the dispatcher gains the D163 marker rule (honourQuietHoursOverride: a pre-retry timing choice is spent, policy re-applies fresh) and its hold branch stays as the policy backstop; dialogue entrance is the tier-3 200ms --motion-out spec, reduced-motion covered by the global kill; no migrations | `a902b55`…`be2bed4` + fix round `e8c748f` on `ui/session-33-quiet-stamp` | closed pending merge — 386 smokes green incl. the seven session-33 items; typecheck + production build clean; live Supabase untouched; DECISIONS 184 verbatim (in-prompt authority), 185 provisional (D176) with (d) amended by founder fix-request at click-review (7 Aug: No-quiet-hours is dispatch-only, the opening-hours fact stands) — the founder's merge is the stamp |
| 34 | 8 Aug 2026 | MCP server over Barakah, read-only (D188 recorded verbatim on in-prompt authority; the founder's mid-session GO approved the 0045 shape and the OAuth recommendation with two riders) — 0045 the MCP read door (mcp_credentials one-live-per-business, hashed at rest, human-minted, machine-actor enforced, immutable with permanent revocation; mcp_tokens hashed/expiring/one-time/service-only; mcp_clients platform registrations; three Spec 3 tool rows record/workflows/memory; mcp_whoami + ten grant-checked security-definer read doors returning compact ISO-stamped JSON, refusals naming the grant); the @rooshni/db mcp module (the area_noun registry with one-line connector descriptions, callMcpTool eventing mcp.tool_called as "Claude via MCP" with summarised payloads, the mint/revoke lifecycle, the OAuth wrapper with PKCE codes and rotating short-lived tokens per the founder's expiry rider); apps/web /api/mcp streamable-HTTP JSON-RPC route failing closed on the bearer with OAuth discovery and per-credential rate limiting, RFC 8414/9728 metadata via rewrites, DCR register, the token endpoint, and the session-gated honest-consent authorise page (rider 2's register); Settings → Integrations gains the Claude (MCP) row (owner mint shown once, permanent revoke, endpoint URL for copying, connection chip EARNED by a recorded call); docs/MCP.md (connect walkthrough, tool table, laws, the growth rule) | `8995953`…`ad38ba0` on `feat/session-34-mcp-server` | closed pending merge — 420 smokes green incl. the 25 session-34 items and both tripwires; typecheck + production build clean; live Supabase untouched (0045 apply + credential mint/connector add are founder steps on GO-LIVE); DECISIONS 188 verbatim (in-prompt authority), 189 provisional (D176) — the founder's merge is the stamp; PLAYBOOK §7 gains the MCP read door at close |
